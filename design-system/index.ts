/**
 * Design System
 * ─────────────────────────────────────────
 * Next.js 16 + TypeScript + Tailwind CSS v4
 *
 * Usage:
 *   import { Button, Badge, Card } from '@/design-system'
 *   import { colors, typography, spacing } from '@/design-system/tokens'
 *   import { Stack, HStack, VStack, Grid } from '@/design-system/patterns'
 *   import { SearchIcon, CheckIcon } from '@/design-system/icons'
 */

// Tokens
export * from './tokens';

// Components
export * from './components';

// Patterns
export * from './patterns';

// Icons
export * from './icons';
